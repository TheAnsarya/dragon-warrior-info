# Dragon Warrior ROM Patches

This directory contains IPS and BPS patch files that track differences between the original Dragon Warrior ROM and ROMs built with this project.

## Patch Files

### Automatic Timestamped Patches

- `dragon_warrior_YYYYMMDD_HHMMSS.ips` - Automatically generated IPS patches with build timestamps
- `dragon_warrior_YYYYMMDD_HHMMSS.bps` - Automatically generated BPS patches with build timestamps

### Manual Patches

- `dragon_warrior_modifications.ips` - Manually generated IPS patch format (compatible with most ROM patchers)
- `dragon_warrior_modifications.bps` - Manually generated BPS patch format (newer format with better compression)

## Automatic Patch Generation

**Every ROM build automatically creates timestamped patches!**

When you build a ROM using the build system:

1. ✅ ROM is successfully assembled
2. 🔍 System checks for reference ROM (`dragon_warrior.nes`)  
3. 📊 Compares built ROM with original
4. 📄 **Automatically creates timestamped IPS and BPS patches**
5. 💾 Saves to `patches/dragon_warrior_YYYYMMDD_HHMMSS.{ips,bps}`

**Example automatic patch files:**

```text
patches/
├── dragon_warrior_20251119_142305.ips
├── dragon_warrior_20251119_142305.bps
├── dragon_warrior_20251119_151202.ips  
└── dragon_warrior_20251119_151202.bps
```

## Usage

### Applying Patches

**IPS Patches:**

- Use any IPS patcher (Lunar IPS, Rom Patcher JS, etc.)
- Apply to original Dragon Warrior ROM

**BPS Patches:**

- Use beat, flips, or Rom Patcher JS
- Apply to original Dragon Warrior ROM

### Manual Patch Generation

Patches are also automatically generated by the build system when:

1. You have an original ROM file (`dragon_warrior.nes` or similar)
2. You build a modified ROM using the asset pipeline
3. The build system detects differences between original and built ROM

```bash
# Manual patch generation (if needed)
python tools/patch_generator.py dragon_warrior.nes output/dragon_warrior_modified.nes
```

## Why Track Patches?

- **Version Control**: Track exact changes made to game data
- **Distribution**: Share modifications without distributing copyrighted ROM files  
- **Debugging**: Identify what changed between builds
- **Collaboration**: Other developers can see your modifications

## Important Notes

- Original ROM files are **not** stored in this repository
- Only patch files (differences) are tracked in git
- This respects copyright while preserving modification history
- Users must provide their own legal ROM files
